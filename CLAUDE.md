# 메르 리더 프로젝트

## 프로젝트 목적

메르 블로그 글들을 섹터별 타임라인으로 정리하고, 각 글을 깊이 이해할 수 있도록 해설·풀이를 추가한 **학습용 리더**를 만든다.

요약 도구가 아니다. 메르의 서사·연결고리·관점이 100% 보존된 상태에서, 빠르게 스캔도 되고 깊이 학습도 되는 도구다.

자소서·면접 매핑은 이번 작업에서 하지 않는다. 순수하게 인사이트 학습용.

---

## 입력 자료 구조

```
input/
├── 한국경제·정책/
│   ├── 한국경제·정책.txt       ← 메르 글 N개가 통합된 원본
│   └── 한국경제·정책.html       ← 기존 결과물 (참조용, 보수 X)
├── 환율·외환시장/
│   ├── 환율·외환시장.txt
│   └── 환율·외환시장.html
├── ... (총 13개 섹터)
```

각 .txt 파일 내부 구조:
```
\========================
방위산업 (20개)
\========================

[1] 방위산업 근황 (feat 천무, 폴란드 5.6조원 3차계약)

날짜: 2025. 12. 31 | 링크: https://blog.naver.com/...

\------------------------

1. 방위산업에 대해서 처음 글을 쓴 것이 22년 11월에 쓴 아래 글이었음.
2. ...

[2] 다음 글 제목

날짜: 2025. XX. XX | 링크: ...

\------------------------

1. ...
```

`[숫자]` 패턴이 글의 시작이고, 다음 `[숫자]` 또는 파일 끝까지가 한 글의 본문이다.

---

## 처리 단계

### 1단계: 글 단위 분리

각 섹터의 .txt를 글 단위로 쪼개서 `parsed/{섹터}/{섹터}_{글번호}.txt`로 저장.

분리 기준:
- `[숫자]` 패턴으로 글 시작 인식
- 다음 `[숫자]` 또는 파일 끝까지가 한 글
- 메타 (날짜, 링크)는 글 본문에 포함

### 2단계: 글별 카드 생성 (본 작업)

**가장 중요한 원칙: 글 1개 = API 호출 1개**

글 1개를 처리할 때 다른 글 정보 절대 끌어오지 않는다. 매 호출마다 컨텍스트가 완전히 초기화되어야 한다.

각 글마다 아래 JSON 생성 → `processed/{섹터}/{섹터}_{글번호}.json`

```json
{
  "섹터_원래": "조선·방위산업",
  "섹터_통합": "중후장대",
  "글_번호": 1,
  "글_제목": "폴란드 천무 5.6조원 3차계약 — 현지생산·부품공급 구조로 심화",
  "원본_제목": "방위산업 근황 (feat 천무, 폴란드 5.6조원 3차계약)",
  "날짜": "2025-12-31",
  "원본_링크": "https://blog.naver.com/...",
  "태그": ["#천무", "#폴란드", "#한화에어로", "#자폭드론"],
  "이_글이_말하는_것": {
    "도입": "...",
    "도입_주석": [],
    "단락1": {
      "제목": "폴란드의 구조적 동력",
      "본문": "...",
      "주석": [
        {"유형": "용어풀이", "대상": "레오파르트 2", "내용": "독일제 주력 전차. 연 50대 생산 체제로 2038년까지 수주 포화 상태."},
        {"유형": "맥락풀이", "내용": "이 납기 문제가 K2 선택의 결정적 이유."}
      ]
    },
    "단락2": {
      "제목": "K2가 자리잡은 이유",
      "본문": "...",
      "주석": []
    },
    "단락3": {
      "제목": "전장의 무게중심 이동",
      "본문": "...",
      "주석": []
    }
  },
  "메르가_던지는_질문": "재래식 방산에서 승자가 되어가는 한국이...",
  "나의_답": "한국은 아직 재래식의 단가·품질 경쟁에 머물러 있고..."
}
```

### 3단계: HTML 렌더링

JSON 모아서 통합 폴더 8개 기준으로 HTML 타임라인 페이지 생성.

---

## 글_제목 작성 규칙

- `원본_제목`: 메르가 쓴 블로그 원문 제목 그대로
- `글_제목`: 핵심 내용이 한눈에 보이도록 재작성
  - 핵심 사실 + 그 안에 담긴 긴장·함의를 자연스럽게 담기
  - 형식 고정 금지 — 단정·의문·병치 등 글마다 가장 읽기 좋은 형태로
  - 핵심 숫자·고유명사 살리기, 평가어("놀랍다", "충격적") 금지
  - 예: `천무 현지생산으로 들어갔는데, EU 방산 재건 전에 잠금 걸 수 있나`
  - 예: `이라크에 천궁 팔았는데, 이게 이스라엘 이란 공격로 막는 거 아닌가`
  - 예: `K2 연 200대 뽑는 나라가, 레오파르트 연 50대 독일을 제쳤다`
  - 30자 내외

---

## "이 글이 말하는 것" 작성 규칙

### 구조
- 도입 단락 1개 + 소제목 단락 3개 (글에 따라 2~4개 허용)
- 도입: 시점·전체 그림 + 이 장면이 보여주는 것 (2~3문장)
- 각 소제목 단락: 3~4문장
- 소제목 형식: `1. 짧은 핵심 구` (의문문·평가어 금지)
  - 좋은 예: "폴란드의 구조적 동력", "K2가 자리잡은 이유"
  - 나쁜 예: "왜 폴란드인가?", "K2의 놀라운 성공"

### 분량
- 전체 약 450~550자
- 너무 짧으면 단편적, 너무 길면 원문 펼치는 의미 없음

### 톤
- **보고서·요약서 톤** (객관 서술)
- **메르를 주어로 한 인용 표현 절대 금지**
  - ❌ "메르가 짚는 핵심은", "메르의 비유로는", "메르가 이 글에서"
  - ✅ 사실 자체로 서술
- 인라인 번호 ("첫째/둘째/셋째") 금지 — 소제목으로 대체됨

### 필수
- 도입 단락에 시계열 위치 명시 (예: "22년 11월 첫 글 시점에", "3년 뒤인 현재")
- 핵심 숫자·고유명사 본문에 자연스럽게 녹이기
- 메르 특유 비유·표현은 인용부호 없이 자연 서술
  (예: "한국이 일본 보는 시각의 100배" 같은 표현은 그대로 살리되 ""로 감싸지 않음)
- 각 단락 마지막에 한 호흡 더 풀어주는 문장 1개 (왜 중요한지·무엇을 의미하는지)

### 단락 주석 (주석 필드)
각 단락 본문에서 언급된 용어·개념에 한해, 본문 바로 아래 `주석` 배열로 추가.

- `유형`: `"용어풀이"` 또는 `"맥락풀이"`
- `용어풀이`: `대상` 필드에 해당 단어, `내용`에 1~2줄 사실 정보
- `맥락풀이`: `내용`에 글 내부 인과·연결고리 1~2줄
- 주석이 없는 단락은 `"주석": []`
- 도입에도 필요하면 `도입_주석` 배열 사용
- 과잉 주석 금지 — 진짜 필요한 것만

### 금지
- 본인 의견·평가 ("주목할 만하다", "시사점이 크다" 등)
- 결론 뭉개기 ("향후 동향이 중요하다" 등)
- 메르가 안 쓴 내용 추가 (사실 추측 금지)
- 자소서·면접·지원기업 매핑 (이번 작업에서는 안 함)

---

## "메르가 던지는 진짜 질문" + "나의 답" 작성 규칙

### 메르 질문
- 1문장, 질문 형식 유지
- 메르 글의 진짜 핵심 통찰을 질문 형태로 추출
- 글에서 명시적으로 던진 질문이 없으면, 글 전체가 던지는 함의를 질문으로 변환

### 나의 답
- 2~3문장 (4문장 넘기지 말 것)
- 본인 입장을 짧고 단호하게
- **양시론·뭉개는 표현 절대 금지**
  - ❌ "신중한 접근이 필요하다", "양면이 있다", "균형이 중요하다"
  - ✅ "X가 부족하다, 지금부터 Y를 깔아야 한다"
- 본문에 없는 큰 추측·외부 데이터 동원 금지
- 글이 던진 질문에 대한 본인 입장 정리, 그 이상은 X

---

## 통합 폴더 매핑

13개 원래 섹터 → 8개 통합 폴더로 매핑.
처리는 글 단위로 하되, JSON에 두 정보 다 기록.

| 원래 섹터 | 통합 폴더 |
|---|---|
| 금리·채권·통화정책 | 금융·통화 |
| 환율·외환시장 | 금융·통화 |
| 중동·지정학 | 지정학 |
| 중국경제·정치 | 지정학 |
| 미국경제·정책 | 미국·글로벌 |
| 글로벌 기타 | 미국·글로벌 |
| 한국경제·정책 | 한국경제 |
| 부동산·건설·PF | 부동산 |
| 반도체·AI·기술 | 기술·산업 |
| 배터리·전기차 | 기술·산업 |
| 조선·방위산업 | 중후장대 |
| 주식·투자·금융 | 자본시장 |

---

## 모델·비용 정책

### 사용 모델
- **Sonnet 4.6 + 적응형 사고**
- 모델명: `claude-sonnet-4-6`
- effort: 기본값(adaptive 활성화)

### 캐싱
- 시스템 프롬프트(이 CLAUDE.md 규칙 + 카드 포맷 템플릿): 캐시 처리
- 글 본문: 매번 새로 (캐시 X)

### 진행 관리
- `progress.log`: 처리 완료된 글 번호 기록
- `errors.log`: 실패한 글 + 사유 기록
- 재시작 시 progress.log에 기록된 글은 건너뜀
- 한 글 처리 실패해도 전체 중단 X, 다음 글로 진행

---

## 실행 명령어 (사용자가 호출할 명령)

### 1. 글 분리
```
1단계 시작: input/ 폴더 13개 섹터의 .txt를 글 단위로 분리해서 parsed/에 저장해줘.
```

### 2. 파일럿 (1개 섹터만)
```
파일럿: 조선·방위산업 섹터의 글들만 처리해서 processed/중후장대/에 JSON 저장하고, 
샘플 HTML 1개 만들어서 보여줘.
```

### 3. 일괄 처리
```
일괄 처리 시작: 나머지 섹터 모두 처리해줘. 글 1개 = 호출 1개 원칙 엄수.
중간에 멈췄다 재시작해도 처리된 글은 건너뛰도록.
```

### 4. 렌더링
```
렌더링: processed/의 모든 JSON을 읽어서 통합 폴더 8개 기준으로 
HTML 타임라인 페이지 생성. 카드 클릭 시 펼침, 시간순 오름차순.
```

---

## 출력 폴더 구조 (최종)

```
~/메르_리더/
├── CLAUDE.md
├── input/                       ← 원본 (수정 X)
├── parsed/                      ← 글 단위 분리 결과
│   ├── 조선·방위산업/
│   │   ├── 조선·방위산업_001.txt
│   │   └── ...
├── processed/                   ← 글별 JSON
│   ├── 중후장대/
│   │   ├── 조선·방위산업_001.json
│   │   └── ...
│   ├── 금융·통화/
│   └── ...
├── output/                      ← 최종 HTML
│   ├── index.html               ← 8개 통합 폴더 한눈에
│   ├── 중후장대/index.html      ← 타임라인 페이지
│   ├── 금융·통화/index.html
│   └── ...
├── progress.log
└── errors.log
```
