name: Daily 메르 블로그 업데이트

on:
  schedule:
    - cron: '0 21 * * *'  # 매일 오전 6시 KST (UTC 21:00)
  workflow_dispatch:
    inputs:
      mode:
        description: 'deploy-only (기존 HTML만 배포) 또는 full (수집+배포)'
        required: false
        default: 'full'

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Python 설정
        if: ${{ github.event.inputs.mode != 'deploy-only' }}
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 의존성 설치
        if: ${{ github.event.inputs.mode != 'deploy-only' }}
        run: pip install openai aiofiles

      - name: 새 글 수집 및 처리
        if: ${{ github.event.inputs.mode != 'deploy-only' }}
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          if [ ! -f new_posts.log ]; then
            echo "최초 실행: 2025-04-06 이후 백필"
            python crawl_new.py --backfill 2025-04-06
          else
            python crawl_new.py
          fi

      - name: 변경사항 커밋 및 푸시
        if: ${{ github.event.inputs.mode != 'deploy-only' }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add processed/ output/ new_posts.log progress.log errors.log 2>/dev/null || true
          git diff --staged --quiet || git commit -m "chore: 메르 블로그 자동 업데이트 $(date '+%Y-%m-%d')"
          git push || true

      - name: GitHub Pages 설정
        uses: actions/configure-pages@v4

      - name: output/ 업로드
        uses: actions/upload-pages-artifact@v3
        with:
          path: output/

      - name: 배포
        id: deployment
        uses: actions/deploy-pages@v4
